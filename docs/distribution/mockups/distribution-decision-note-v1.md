Distribution v1 · Step 2 decision note (Lane 1, 2026-04-26)

1. Atoms new: OverdueChip (header atom, two states zero/engaged with pulsing dot when count>0), ZoneDot (6px functional marker, 5 variants per --zone-* token delta), UpstreamReadinessRow (room+stage+days-to-ready, three states landing/normal/stuck), per-zone RouteRow with active/empty driver-empty states, dragover DayCell affordance (2px --accent left rule + 1px interior, no fill no scale).
2. Atoms reused: KpiTile from System Reference section 05, hairline-grid pattern from PHASE-1-SURFACE-GUIDE.md, gapped-card pattern, inner-row hairline-divider pattern, stage-dot 6px pattern, document pill (sent/pending/overdue), tabular-numerals .tnum class on every numeric cell.
3. Tokens touched: zero existing tokens modified. Added five new --zone-* tokens (--zone-local #B5A88B, --zone-east-valley #B07670, --zone-west-valley #5F7385, --zone-north-valley #7A9678, --zone-other #6B6B70) in distribution-token-delta-v1.css for merge into brand-tokens.css after Justin review.
4. Question answered: the calendar surface is hairline (A), per the v7 doctrine — drag affordance, hover, selection, dragover, and overdue-day-marker all expressed via 2px left accent rules (--accent engaged, --status-warn stuck, --status-bad overdue), not background fills, not scale transforms; this preserves scan speed across the 35-cell grid while keeping interactions legible. Confirmed: all three treatments coexist on this surface (A on calendar/mini-cal/KPIs, B on day cards and panel containers, C on RouteSummaryPanel/Upstream/InProduction interiors and expanded OrderReadinessCard).
5. Question open: should the Upstream Readiness "stuck" state route directly to the Cultivation surface for the operator (Andrew handoff), or should the row remain read-only-with-tooltip in v1 and queue the cross-module navigation for v2 with the dry/cure-built-in workflow? Recommendation: read-only in v1, file the cross-module link as a small follow-up; this keeps the v1 Upstream cell scope to "surface signal only" per spec decision 3.
